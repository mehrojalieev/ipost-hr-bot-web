# ⚠️ DEMO REJIMI — vaqtincha OCHIQ (MVP ko'rsatish uchun).
# Bu holatda panel hammaga ochiq — Telegram/HR-ID tekshiruvi o'chirilgan.
#
# XAVFSIZLASH (demo tugagach):
#   1) Vercel → Settings → Environment Variables → NEXT_PUBLIC_DEV_BYPASS=false
#      (dashboard qiymati bu fayldan ustun turadi), + BOT_TOKEN va HR_TELEGRAM_IDS qo'shing
#   2) yoki shu faylni o'chirib, qayta deploy qiling
NEXT_PUBLIC_DEV_BYPASS=false
